#!/usr/bin/env node
/**
 * Register OpenCode skills from the active Harnessy skills root.
 * Auto-generated by flow-install.
 */
import { GLOBAL_SKILLS_DIR, registerOpenCodeSkills } from "./agents.mjs";

const run = async () => {
  const count = await registerOpenCodeSkills(GLOBAL_SKILLS_DIR);
  console.log("OpenCode skills.paths configured (" + count + " path(s))");
};

run().catch((e) => {
  console.error("OpenCode registration failed:", e);
  process.exitCode = 1;
});
