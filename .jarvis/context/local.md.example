# Local Context (Machine-Specific)

Copy this file to `local.md` and fill in your values.
`local.md` is gitignored -- never commit it.

## External Projects

| Project | Local Path | Notes |
|---|---|---|
| Jarvis CLI | | Python 3.11+ AI assistant CLI. Use `uv run jarvis <command>` |

## Environment Notes

- Node version manager: nvm / fnm / volta
- Python environment: uv
- Rust toolchain: rustup
