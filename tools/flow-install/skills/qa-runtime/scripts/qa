#!/usr/bin/env node

import { runCli } from "./qa-runtime-lib.mjs";

const exitCode = await runCli(process.argv.slice(2), {
  commandName: "qa",
  cwd: process.cwd(),
  stdout: process.stdout,
  stderr: process.stderr,
});

process.exit(exitCode);
