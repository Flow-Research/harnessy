{
  "name": "@harnessy/skill-registry",
  "version": "0.0.1",
  "private": true,
  "description": "Cloudflare Worker that provisions per-skill Artifacts repos and mints scoped access tokens.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
